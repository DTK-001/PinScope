// PinScope cloud course sync configuration.
//
// Public users only need the endpoint so their devices can read the published
// course library. Do NOT hard-code your admin token here in a public build;
// save the token on your own admin device through the in-app Cloud course sync
// panel or by opening the app with ?pinscopeSyncToken=YOUR_TOKEN once.

export const pinscopeSyncConfig = {
  endpoint: "",
  adminToken: ""
};
